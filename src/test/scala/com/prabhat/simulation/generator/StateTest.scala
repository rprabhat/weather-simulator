package com.prabhat.simulation.generator

import org.scalatest.{Matchers, FlatSpec}

import scala.util.Random

/**
  * Created by prabhat on 24/08/2016.
  */
class StateTest extends FlatSpec with Matchers {

//  "map on State" should "transform it" in {
//    State[Integer, Double]( Integer => (Double , Integer)) map(_.toString()) shouldEqual State( Integer => (String, Integer))
//  }

}
